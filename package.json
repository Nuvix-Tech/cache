{
  "name": "@nuvix/cache",
  "version": "1.0.0",
  "description": "A flexible caching library with adapter support for different storage backends.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint . --ext .ts",
    "test": "jest",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nuvix/cache.git"
  },
  "keywords": [
    "cache",
    "adapter",
    "redis",
    "performance",
    "nuvix"
  ],
  "author": "Nuvix",
  "license": "BSD-3-Clause",
  "dependencies": {
  },
  "devDependencies": {
    "ioredis": "^5.6.0",
    "typescript": "^5.2.0",
    "eslint": "^8.53.0",
    "jest": "^29.6.2",
    "ts-jest": "^29.1.0"
  },
  "peerDependencies": {
    "@nuvix/telemetry": "^1.0.0-alpha.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}